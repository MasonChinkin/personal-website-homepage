import React from "react";
import ReactDom from 'react-dom';
import Projects from "./Projects";
jest.mock('../../../util/imageLoaderMock.js')

it("renders without crashing", () => {
  // shallow(<Projects />);
  return undefined;
});